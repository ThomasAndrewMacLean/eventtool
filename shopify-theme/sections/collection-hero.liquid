<div class="hero hero--sub" id="hero">
    <div class="container">
        <div class="sub-header">
            <div class="text--label">{{section.settings.title}}</div>
            <h1 class="text--h1">{{collection.title}}</h1>
        </div>
    </div>
</div>

{% schema %}
{
    "name": "Collection hero",
    "settings": [
        {
            "type": "text",
            "id": "title",
            "label": "Collection Title",
            "default": "Collection" 
        }
    ]
      
  }
{% endschema %}